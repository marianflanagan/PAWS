<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marianflanagan.paws">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".home.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.ActivityLogin" />
        <activity android:name=".customer.CustomerLogin" />
        <activity android:name=".pet_sitter.PetSitterLogin" />
        <activity android:name=".customer.CustomerRegistration" />
        <activity android:name=".pet_sitter.PetSitterRegistration" />
        <activity android:name=".customer.CustomerProfile" />
        <activity android:name=".pet_sitter.PetSitterProfile" />
        <activity android:name=".customer.BookingServices" />
        <activity android:name=".customer.PetDetails" />
        <activity android:name=".customer.PetSitterSelection" />
        <activity android:name=".customer.EditCustomerDetails" />
        <activity android:name=".pet_sitter.ViewBookings" />
        <activity android:name=".pet_sitter.EditPetSitterDetails" />
        <activity android:name=".customer.Calendar" />
        <activity android:name=".customer.MyPets" />
        <activity android:name=".customer.CustomerViewBookings"/>
        <activity android:name=".booking.BookingSummaryActivity"/>
    </application>

</manifest>